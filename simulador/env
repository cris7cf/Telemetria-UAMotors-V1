# Dirección del gateway local
GATEWAY_URL=http://localhost:5000/telemetria

# Identificador del dispositivo (para distinguir varios ESP32)
DEVICE_ID=esp32-sim-01

# Intervalo de envío (segundos)
SEND_INTERVAL_S=1.0

# Precisión de la marca de tiempo (ns, ms, s)
TIME_PRECISION=ns